const React = require("react-native");

const { StyleSheet } = React;

export default {
    container: {
        backgroundColor: "#000",
    },
    content: {
        height: "100%",
        display: 'flex',
        backgroundColor: '#fff',
        justifyContent: 'space-around'
    }
};